version: v1.0
name: Publish ALP images
agent:
  machine:
    type: f1-standard-2
    os_image: ubuntu2204
execution_time_limit:
  minutes: 60
global_job_config:
  env_vars:
    - name: DEV_REGISTRIES
      value: gcr.io/unique-caldron-775/cnx/tigera quay.io/tigeradev
  secrets:
    - name: docker-hub
    - name: google-service-account-for-gcr
    - name: quay-tigeradev-hashrelease
  prologue:
    commands:
      - checkout
      # Semaphore is doing shallow clone on a commit without tags.
      # unshallow it for GIT_VERSION:=$(shell git describe --tags --dirty --always)
      - retry git fetch --unshallow
      - echo $DOCKERHUB_PASSWORD | docker login --username "$DOCKERHUB_USERNAME" --password-stdin
      - echo $QUAY_TOKEN | docker login --username "$QUAY_USER" --password-stdin quay.io
      - docker login --username casey@tigera.io -u _json_key -p "$(cat ~/secrets/secret.google-service-account-key.json)" https://gcr.io
      - export BRANCH_NAME=$SEMAPHORE_GIT_BRANCH
      - ssh-add ~/.ssh/id_rsa
blocks:
  - name: Publish ALP images
    dependencies: []
    skip:
      when: "branch !~ '.+'"
    task:
      prologue:
        commands:
          # Restore cached geoip database
          - cache restore app-policy-geoip
      jobs:
        - name: Linux amd64
          commands:
            - if [ -z "${SEMAPHORE_GIT_PR_NUMBER}" ]; then make -C app-policy cd ARCHES=amd64 CONFIRM=true; fi
  - name: Publish ALP images - native arm64 runner
    dependencies: []
    skip:
      when: "branch !~ '.+'"
    task:
      agent:
        machine:
          type: s1-aws-arm64-2
      prologue:
        commands:
          # Restore cached geoip database
          - cache restore app-policy-geoip
      jobs:
        - name: Linux arm64
          commands:
            - if [ -z "${SEMAPHORE_GIT_PR_NUMBER}" ]; then make -C app-policy cd ARCHES=arm64 CONFIRM=true; fi
  - name: Publish ALP multi-arch manifests
    dependencies:
      - Publish ALP images
      - "Publish ALP images - native arm64 runner"
    skip:
      when: "branch !~ '.+'"
    task:
      jobs:
        - name: Linux multi-arch manifests
          commands:
            - if [ -z "${SEMAPHORE_GIT_PR_NUMBER}" ]; then make -C app-policy push-manifests-with-tag CONFIRM=true; fi
