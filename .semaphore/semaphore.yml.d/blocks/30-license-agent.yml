- name: License Agent
  run:
    when: "${CHANGE_IN(license-agent)}"
  dependencies:
    - Prerequisites
  task:
    prologue:
      commands:
        - cd license-agent
    jobs:
      - name: "License Agent: CI"
        commands:
          - ../.semaphore/run-and-monitor ci.log make ci
      - name: "License Agent: build arm64"
        matrix:
          - env_var: ARCH
            values:
              - arm64
        commands:
          - ../.semaphore/run-and-monitor build-$ARCH.log make build ARCH=$ARCH
    epilogue:
      always:
        commands:
          - 'test-results publish --name "${SEMAPHORE_JOB_NAME}" ./report/*.xml || true'
