- name: es-gateway
  run:
    when: "${CHANGE_IN(es-gateway)}"
  dependencies:
    - Prerequisites
  task:
    prologue:
      commands:
        - cd es-gateway
    jobs:
      - name: "es-gateway: CI"
        commands:
          - ../.semaphore/run-and-monitor ci.log make ci
      - name: "es-gateway: build arm64"
        matrix:
          - env_var: ARCH
            values:
              - arm64
        commands:
          - ../.semaphore/run-and-monitor build-$ARCH.log make build ARCH=$ARCH
    epilogue:
      always:
        commands:
          - 'test-results publish --name "${SEMAPHORE_JOB_NAME}" ./report/*.xml || true'
