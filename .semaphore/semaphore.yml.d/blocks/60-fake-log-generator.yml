- name: Fake log generator
  run:
    when: "${CHANGE_IN(test-tools/fake-log-generator)}"
  dependencies:
    - Prerequisites
  task:
    prologue:
      commands:
        - cd test-tools/fake-log-generator
    jobs:
      - name: "Fake log generator: CI"
        commands:
          - ../../.semaphore/run-and-monitor make-ci.log make ci
