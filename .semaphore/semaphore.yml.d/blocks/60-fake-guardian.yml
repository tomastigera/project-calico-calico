- name: Fake guardian
  run:
    when: "${CHANGE_IN(test-tools/fake-guardian)}"
  dependencies:
    - Prerequisites
  task:
    prologue:
      commands:
        - cd test-tools/fake-guardian
    jobs:
      - name: "Fake guardian: CI"
        commands:
          - ../../.semaphore/run-and-monitor make-ci.log make ci
