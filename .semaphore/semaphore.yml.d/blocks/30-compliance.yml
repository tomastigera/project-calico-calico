- name: Compliance
  run:
    when: "${CHANGE_IN(compliance)}"
  dependencies:
    - Prerequisites
  task:
    prologue:
      commands:
        - cd compliance
    jobs:
      - name: "Compliance: CI"
        commands:
          - ../.semaphore/run-and-monitor ci.log make ci
      - name: "Compliance: build arm64"
        matrix:
          - env_var: ARCH
            values:
              - arm64
        commands:
          - ../.semaphore/run-and-monitor build-$ARCH.log make build ARCH=$ARCH
    epilogue:
      always:
        commands:
          - 'test-results publish ./report/*.xml --name "Compliance: UT" || true'
