- name: Policy Recommendation
  run:
    when: "${CHANGE_IN(policy-recommendation)}"
  dependencies:
    - Prerequisites
  task:
    prologue:
      commands:
        - cd policy-recommendation
    jobs:
      - name: "Policy Recommendation: CI"
        commands:
          - ../.semaphore/run-and-monitor ci.log make ci
      - name: "Policy Recommendation: build arm64"
        matrix:
          - env_var: ARCH
            values:
              - arm64
        commands:
          - ../.semaphore/run-and-monitor build-$ARCH.log make build ARCH=$ARCH
    epilogue:
      always:
        commands:
          - 'test-results publish ./report/*.xml --name "Policy recommendation: UT" || true'
