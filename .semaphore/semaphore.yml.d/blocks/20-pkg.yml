- name: pkg
  run:
    when: "${FORCE_RUN} or change_in(['/pkg/'])"
  dependencies:
    - Prerequisites
  task:
    prologue:
      commands:
        - cd pkg
    jobs:
      - name: make ci
        commands:
          - ../.semaphore/run-and-monitor make-ci.log make ci
