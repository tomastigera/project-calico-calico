- name: Ingress collector
  run:
    when: "${CHANGE_IN(ingress-collector)}"
  dependencies:
    - Prerequisites
  task:
    prologue:
      commands:
        - cd ingress-collector
    jobs:
      - name: "Ingress collector: CI"
        commands:
          - ../.semaphore/run-and-monitor ci.log make ci
      - name: "Ingress collector: build arm64"
        matrix:
          - env_var: ARCH
            values:
              - arm64
        commands:
          - ../.semaphore/run-and-monitor build-$ARCH.log make build ARCH=$ARCH
    epilogue:
      always:
        commands:
          - 'test-results publish --name "${SEMAPHORE_JOB_NAME}" ./report/*.xml || true'
