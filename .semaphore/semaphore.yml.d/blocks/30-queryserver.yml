- name: queryserver
  run:
    when: "${CHANGE_IN(queryserver)}"
  dependencies:
    - Prerequisites
  task:
    prologue:
      commands:
        - cd queryserver
    jobs:
      - name: queryserver tests
        commands:
          - ../.semaphore/run-and-monitor ci.log make ci
      - name: Build binary
        matrix:
          - env_var: ARCH
            values:
              - arm64
        commands:
          - ../.semaphore/run-and-monitor build-$ARCH.log make build ARCH=$ARCH
