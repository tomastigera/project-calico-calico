# !! WARNING, DO NOT EDIT !! This file is generated from the templates
# in /.semaphore/semaphore.yml.d. To update, modify the relevant
# template and then run 'make gen-semaphore-yaml'.
version: v1.0
name: Calico
execution_time_limit:
  hours: 4
agent:
  machine:
    type: f1-standard-2
    os_image: ubuntu2204
auto_cancel:
  running:
    when: "branch != 'master'"
  queued:
    when: "branch != 'master'"
global_job_config:
  secrets:
    - name: banzai-secrets
    - name: docker-hub
    - name: private-repo
  prologue:
    commands:
      # Correct permissions since they are too open by default:
      - chmod 0600 ~/.keys/*
      # Remove all keys from the ssh agent so that only the ones from our
      # secret are used.  Without this, ssh may try to use Semaphore's default
      # ssh key, which can only access repos that have Semaphore builds.
      - ssh-add -D
      # Add the key to the ssh agent:
      - ssh-add ~/.keys/*
      - checkout
      - export CALICO_DIR="$(pwd)"
      - export CALICO_DIR_NAME="$(basename "$CALICO_DIR")"
      - mkdir artifacts
      # Semaphore is doing shallow clone on a commit without tags.
      # unshallow it for GIT_VERSION:=$(shell git describe --tags --dirty --always)
      - if [[ "${SEMAPHORE_BLOCK_NAME}" != "Prerequisites" ]]; then retry git fetch --unshallow; fi
      - echo $DOCKERHUB_PASSWORD | docker login --username "$DOCKERHUB_USERNAME" --password-stdin
  epilogue:
    commands:
      - cd "$CALICO_DIR"
      - .semaphore/publish-artifacts
blocks:
  - name: Prerequisites
    run:
      when: "true or change_in(['/'], {pipeline_file: 'track'})"
    dependencies: []
    task:
      agent:
        machine:
          type: f1-standard-2
          os_image: ubuntu2204
      jobs:
        - name: Pre-flight checks
          commands:
            - make ci-preflight-checks
  - name: Publish tigera/snort3 images
    run:
      when: "true or change_in(['/third_party/snort3'], {pipeline_file: 'ignore', exclude: ['/**/.gitignore', '/**/README.md', '/**/LICENSE'], pipeline_file: 'ignore'})"
    dependencies:
      - Prerequisites
    task:
      agent:
        machine:
          type: f1-standard-4
          os_image: ubuntu2204
      secrets:
        - name: google-service-account-for-gcr
      prologue:
        commands:
          - docker login --username casey@tigera.io -u _json_key -p "$(cat ~/secrets/secret.google-service-account-key.json)" https://gcr.io
      jobs:
        - name: "Publish tigera/snort3 images: Linux amd64"
          commands:
            - make -C third_party/snort3 cd ARCHES=amd64 CONFIRM=true
  - name: Publish tigera/snort3 images - native arm64 runner
    run:
      when: "true or change_in(['/third_party/snort3'], {pipeline_file: 'ignore', exclude: ['/**/.gitignore', '/**/README.md', '/**/LICENSE'], pipeline_file: 'ignore'})"
    dependencies:
      - Prerequisites
    task:
      agent:
        machine:
          type: s1-aws-arm64-2
      secrets:
        - name: google-service-account-for-gcr
      prologue:
        commands:
          - docker login --username casey@tigera.io -u _json_key -p "$(cat ~/secrets/secret.google-service-account-key.json)" https://gcr.io
      jobs:
        - name: "Publish tigera/snort3 images: Linux arm64"
          commands:
            - make -C third_party/snort3 cd ARCHES=arm64 CONFIRM=true
  - name: Publish tigera/snort3 multi-arch manifests
    run:
      when: "true or change_in(['/third_party/snort3'], {pipeline_file: 'ignore', exclude: ['/**/.gitignore', '/**/README.md', '/**/LICENSE'], pipeline_file: 'ignore'})"
    dependencies:
      - Publish tigera/snort3 images
      - Publish tigera/snort3 images - native arm64 runner
    task:
      secrets:
        - name: google-service-account-for-gcr
      prologue:
        commands:
          - docker login --username casey@tigera.io -u _json_key -p "$(cat ~/secrets/secret.google-service-account-key.json)" https://gcr.io
      jobs:
        - name: "Publish tigera/snort3 multi-arch manifests: Linux multi-arch"
          commands:
            - make -C third_party/snort3 push-manifest CONFIRM=true
  - name: Deep packet inspection
    run:
      when: "true"
    dependencies:
      - Publish tigera/snort3 multi-arch manifests
    task:
      secrets:
        - name: google-service-account-for-gcr
      prologue:
        commands:
          - docker login --username casey@tigera.io -u _json_key -p "$(cat ~/secrets/secret.google-service-account-key.json)" https://gcr.io
          - cd deep-packet-inspection
      jobs:
        - name: "Deep packet inspection: CI"
          commands:
            - ../.semaphore/run-and-monitor ci.log make ci
        - name: "Deep packet inspection: build arm64"
          matrix:
            - env_var: ARCH
              values:
                - arm64
          commands:
            - ../.semaphore/run-and-monitor build-$ARCH.log make build ARCH=$ARCH
  - name: Publish tigera/elasticsearch images
    run:
      when: "true or change_in(['/third_party/elasticsearch'], {exclude: ['/**/.gitignore', '/**/README.md', '/**/LICENSE'], pipeline_file: 'ignore'})"
    dependencies:
      - Prerequisites
    task:
      agent:
        machine:
          type: f1-standard-4
          os_image: ubuntu2204
      secrets:
        - name: google-service-account-for-gcr
      prologue:
        commands:
          - docker login --username casey@tigera.io -u _json_key -p "$(cat ~/secrets/secret.google-service-account-key.json)" https://gcr.io
      jobs:
        - name: "Publish tigera/elasticsearch images: Linux amd64"
          commands:
            - make -C third_party/elasticsearch cd ARCHES=amd64 CONFIRM=true
  - name: Publish tigera/elasticsearch images - native arm64 runner
    run:
      when: "true or change_in(['/third_party/elasticsearch'], {exclude: ['/**/.gitignore', '/**/README.md', '/**/LICENSE'], pipeline_file: 'ignore'})"
    dependencies:
      - Prerequisites
    task:
      agent:
        machine:
          type: s1-aws-arm64-2
      secrets:
        - name: google-service-account-for-gcr
      prologue:
        commands:
          - docker login --username casey@tigera.io -u _json_key -p "$(cat ~/secrets/secret.google-service-account-key.json)" https://gcr.io
      jobs:
        - name: "Publish tigera/elasticsearch images: Linux arm64"
          commands:
            - make -C third_party/elasticsearch cd ARCHES=arm64 CONFIRM=true
  - name: Publish tigera/elasticsearch multi-arch manifests
    run:
      when: "true or change_in(['/third_party/elasticsearch'], {exclude: ['/**/.gitignore', '/**/README.md', '/**/LICENSE'], pipeline_file: 'ignore'})"
    dependencies:
      - Publish tigera/elasticsearch images
      - Publish tigera/elasticsearch images - native arm64 runner
    task:
      secrets:
        - name: google-service-account-for-gcr
      prologue:
        commands:
          - docker login --username casey@tigera.io -u _json_key -p "$(cat ~/secrets/secret.google-service-account-key.json)" https://gcr.io
      jobs:
        - name: "Publish tigera/elasticsearch multi-arch manifests: Linux multi-arch"
          commands:
            - make -C third_party/elasticsearch push-manifest CONFIRM=true
  - name: Elasticsearch
    run:
      when: "true"
    dependencies:
      - Publish tigera/elasticsearch multi-arch manifests
    task:
      secrets:
        - name: google-service-account-for-gcr
      prologue:
        commands:
          - docker login --username casey@tigera.io -u _json_key -p "$(cat ~/secrets/secret.google-service-account-key.json)" https://gcr.io
          - cd elasticsearch
      jobs:
        - name: "Elasticsearch: CI"
          commands:
            - ../.semaphore/run-and-monitor ci.log make ci
        - name: "Elasticsearch: build arm64"
          matrix:
            - env_var: ARCH
              values:
                - arm64
          commands:
            - ../.semaphore/run-and-monitor build-$ARCH.log make build ARCH=$ARCH
  - name: Publish tigera/fluentd-base images
    run:
      when: "true or change_in(['/third_party/fluentd-base'], {exclude: ['/**/.gitignore', '/**/README.md', '/**/LICENSE'], pipeline_file: 'ignore'})"
    dependencies:
      - Prerequisites
    task:
      secrets:
        - name: google-service-account-for-gcr
      prologue:
        commands:
          - docker login --username casey@tigera.io -u _json_key -p "$(cat ~/secrets/secret.google-service-account-key.json)" https://gcr.io
      jobs:
        - name: "Publish tigera/fluentd-base images: Linux amd64"
          commands:
            - make -C third_party/fluentd-base cd ARCHES=amd64 CONFIRM=true
  - name: Publish tigera/fluentd-base images - native arm64 runner
    run:
      when: "true or change_in(['/third_party/fluentd-base'], {exclude: ['/**/.gitignore', '/**/README.md', '/**/LICENSE'], pipeline_file: 'ignore'})"
    dependencies:
      - Prerequisites
    task:
      agent:
        machine:
          type: s1-aws-arm64-2
      secrets:
        - name: google-service-account-for-gcr
      prologue:
        commands:
          - docker login --username casey@tigera.io -u _json_key -p "$(cat ~/secrets/secret.google-service-account-key.json)" https://gcr.io
      jobs:
        - name: "Publish tigera/fluentd-base images: Linux arm64"
          commands:
            - make -C third_party/fluentd-base cd ARCHES=arm64 CONFIRM=true
  - name: Publish tigera/fluentd-base multi-arch manifests
    run:
      when: "true or change_in(['/third_party/fluentd-base'], {exclude: ['/**/.gitignore', '/**/README.md', '/**/LICENSE'], pipeline_file: 'ignore'})"
    dependencies:
      - Publish tigera/fluentd-base images
      - Publish tigera/fluentd-base images - native arm64 runner
    task:
      secrets:
        - name: google-service-account-for-gcr
      prologue:
        commands:
          - docker login --username casey@tigera.io -u _json_key -p "$(cat ~/secrets/secret.google-service-account-key.json)" https://gcr.io
      jobs:
        - name: "Publish tigera/fluentd-base multi-arch manifests: Linux multi-arch manifests"
          commands:
            - make -C third_party/fluentd-base push-manifest CONFIRM=true
  - name: fluentd
    run:
      when: "true"
    dependencies:
      - Publish tigera/fluentd-base multi-arch manifests
    task:
      secrets:
        - name: google-service-account-for-gcr
      prologue:
        commands:
          - docker login --username casey@tigera.io -u _json_key -p "$(cat ~/secrets/secret.google-service-account-key.json)" https://gcr.io
          - cd fluentd
      jobs:
        - name: "fluentd: CI"
          commands:
            - ../.semaphore/run-and-monitor ci.log make ci
        - name: "fluentd: build arm64"
          matrix:
            - env_var: ARCH
              values:
                - arm64
          commands:
            - ../.semaphore/run-and-monitor build-$ARCH.log make build ARCH=$ARCH
  - name: Publish tigera/fluentd-base Windows images
    run:
      when: "true or change_in(['/third_party/fluentd-base'], {exclude: ['/**/.gitignore', '/**/README.md', '/**/LICENSE'], pipeline_file: 'ignore'})"
    dependencies:
      - Prerequisites
    execution_time_limit:
      hours: 2
    task:
      secrets:
        - name: banzai-secrets
      prologue:
        commands:
          # Login to docker in order to pull images.
          - docker login -u _json_key -p "$(cat ~/secrets/banzai-google-service-account.json)" https://gcr.io
          # Clone the process repo and provision Windows instances.
          - pushd .
          - git clone git@github.com:tigera/process.git ~/process
          - cd ~/process/testing/windows-instances
          - PREFIX=${SEMAPHORE_PROJECT_NAME}-${SEMAPHORE_WORKFLOW_ID} ./create-windows-instances.sh
          # Save windows-instances terraform.
          - tar czf ~/windows-tf.tar.gz -C ~/process/testing/windows-instances .
          - artifact push job --expire-in 2w ~/windows-tf.tar.gz
          - popd
          - cd fluentd
      epilogue:
        always:
          commands:
            - cd ~/process/testing/windows-instances
            - PREFIX=${SEMAPHORE_PROJECT_NAME}-${SEMAPHORE_WORKFLOW_ID} ./create-windows-instances.sh -u
      jobs:
        - name: "Publish tigera/fluentd-base Windows images"
          commands:
            - export PROCESS_REPO=~/process/testing/windows-instances
            - BASE=true CONFIRM=true .semaphore/windows-cd.sh
  - name: Publish tigera/fluentd-base Windows multi-arch manifests
    run:
      when: "true or change_in(['/third_party/fluentd-base'], {exclude: ['/**/.gitignore', '/**/README.md', '/**/LICENSE'], pipeline_file: 'ignore'})"
    dependencies:
      - Publish tigera/fluentd-base Windows images
    task:
      secrets:
        - name: google-service-account-for-gcr
      prologue:
        commands:
          - docker login --username casey@tigera.io -u _json_key -p "$(cat ~/secrets/secret.google-service-account-key.json)" https://gcr.io
      jobs:
        - name: "Publish tigera/fluentd-base Windows multi-arch manifests: Windows multi-arch"
          commands:
            - make -C third_party/fluentd-base push-windows-manifest CONFIRM=true
  - name: fluentd Windows
    run:
      when: "true or change_in(['/fluentd/', '/third_party/fluentd-base'], {exclude: ['/**/.gitignore', '/**/README.md', '/**/LICENSE'], pipeline_file: 'ignore'})"
    dependencies:
      - Publish tigera/fluentd-base Windows multi-arch manifests
    task:
      secrets:
        - name: google-service-account-for-gcr
      prologue:
        commands:
          - docker login --username casey@tigera.io -u _json_key -p "$(cat ~/secrets/secret.google-service-account-key.json)" https://gcr.io
          - cd fluentd
      jobs:
        - name: "fluentd: build windows image"
          commands:
            - ../.semaphore/run-and-monitor build-windows.log make image-windows
  - name: Publish tigera/kibana images
    run:
      when: "true or change_in(['/third_party/kibana'], {exclude: ['/**/.gitignore', '/**/README.md', '/**/LICENSE'], pipeline_file: 'ignore'})"
    dependencies:
      - Prerequisites
    task:
      agent:
        machine:
          type: f1-standard-4
          os_image: ubuntu2204
      secrets:
        - name: google-service-account-for-gcr
      prologue:
        commands:
          - docker login --username casey@tigera.io -u _json_key -p "$(cat ~/secrets/secret.google-service-account-key.json)" https://gcr.io
      jobs:
        - name: "Publish tigera/kibana images: Linux multi-arch"
          commands:
            - make -C third_party/kibana cd CONFIRM=true
  - name: Publish tigera/kibana multi-arch manifests
    run:
      when: "true or change_in(['/third_party/kibana'], {exclude: ['/**/.gitignore', '/**/README.md', '/**/LICENSE'], pipeline_file: 'ignore'})"
    dependencies:
      - Publish tigera/kibana images
    task:
      secrets:
        - name: google-service-account-for-gcr
      prologue:
        commands:
          - docker login --username casey@tigera.io -u _json_key -p "$(cat ~/secrets/secret.google-service-account-key.json)" https://gcr.io
      jobs:
        - name: "Publish tigera/kibana multi-arch manifests: Linux"
          commands:
            - make -C third_party/kibana push-manifest CONFIRM=true
  - name: Kibana
    run:
      when: "true"
    dependencies:
      - Publish tigera/kibana multi-arch manifests
    task:
      secrets:
        - name: google-service-account-for-gcr
      prologue:
        commands:
          - docker login --username casey@tigera.io -u _json_key -p "$(cat ~/secrets/secret.google-service-account-key.json)" https://gcr.io
          - cd kibana
      jobs:
        - name: "Kibana: CI"
          commands:
            - ../.semaphore/run-and-monitor ci.log make ci
